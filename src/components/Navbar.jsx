import React from "react";
import { motion, AnimatePresence } from "framer-motion";
import { NavLink, Link } from "react-router-dom";
import { Menu, X, Sun, Moon } from "lucide-react";
import { useTheme } from "../context/ThemeContext";
const links=[['About','/about'],['Skills','/skills'],['Work','/work'],['Connect','/connect']];
const Navbar=()=>{const {theme,toggleTheme}=useTheme();const [open,setOpen]=React.useState(false);const dark=theme==='dark';return <motion.nav initial={{y:-30,opacity:0}} animate={{y:0,opacity:1}} className={`fixed inset-x-0 top-0 z-50 border-b backdrop-blur-xl ${dark?'border-slate-800/80 bg-slate-950/85':'border-slate-200/80 bg-white/85'}`}><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-6"><Link to="/" className="flex items-center gap-2 font-black tracking-tight"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 text-white">TK</span><span className="hidden sm:block">TSEPO KHOZA<span className="text-blue-500">.</span></span></Link><div className="hidden items-center gap-1 md:flex">{links.map(([name,path])=><NavLink key={path} to={path} className={({isActive})=>`rounded-lg px-4 py-2 text-sm font-medium transition ${isActive?'text-blue-500':dark?'text-slate-300 hover:bg-slate-900 hover:text-white':'text-slate-600 hover:bg-slate-100 hover:text-slate-950'}`}>{name}</NavLink>)}</div><div className="flex items-center gap-2"><button onClick={toggleTheme} aria-label="Toggle theme" className={`rounded-xl p-2.5 ${dark?'bg-slate-900 text-yellow-300':'bg-slate-100 text-slate-700'}`}>{dark?<Sun size={18}/>:<Moon size={18}/>}</button><button onClick={()=>setOpen(!open)} aria-label="Toggle navigation" className={`rounded-xl p-2.5 md:hidden ${dark?'bg-slate-900':'bg-slate-100'}`}>{open?<X size={20}/>:<Menu size={20}/>}</button></div></div><AnimatePresence>{open&&<motion.div initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} className={`overflow-hidden border-t md:hidden ${dark?'border-slate-800 bg-slate-950':'border-slate-200 bg-white'}`}><div className="mx-auto flex max-w-7xl flex-col px-6 py-4">{links.map(([name,path])=><NavLink key={path} onClick={()=>setOpen(false)} to={path} className="rounded-xl px-4 py-3 font-medium hover:bg-blue-500/10 hover:text-blue-500">{name}</NavLink>)}</div></motion.div>}</AnimatePresence></motion.nav>};export default Navbar;
